<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:padding="16dp"
    tools:context=".ui.activities.AnimalActivity">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Dados do Animal"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@+id/tvTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Imagem do Animal -->
            <ImageView
                android:id="@+id/ivAnimalFoto"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_gravity="center_horizontal"
                android:background="@drawable/borda_botoes"
                android:src="@drawable/ic_default_animal"
                android:scaleType="centerCrop"/>

            <!-- Nome -->
            <EditText
                android:id="@+id/etAnimalNome"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/borda_botoes"
                android:hint="Nome do Animal"
                android:textColor="@android:color/white"
                android:textColorHint="#AFAFAF"
                android:padding="12dp" />

            <!-- Espécie e Raça -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:orientation="horizontal">

                <EditText
                    android:id="@+id/etAnimalEspecie"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/borda_botoes"
                    android:hint="Espécie"
                    android:textColor="@android:color/white"
                    android:textColorHint="#AFAFAF"
                    android:padding="12dp" />

                <EditText
                    android:id="@+id/etAnimalRaca"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:layout_weight="1"
                    android:background="@drawable/borda_botoes"
                    android:hint="Raça"
                    android:textColor="@android:color/white"
                    android:textColorHint="#AFAFAF"
                    android:padding="12dp" />
            </LinearLayout>

            <!-- Data de Nascimento -->
            <EditText
                android:id="@+id/etAnimalData"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/borda_botoes"
                android:hint="Data de Nascimento"
                android:textColor="@android:color/white"
                android:textColorHint="#AFAFAF"
                android:padding="12dp"
                android:focusable="false"
                android:clickable="true" />

            <!-- Número do Chip -->
            <EditText
                android:id="@+id/etAnimalChip"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/borda_botoes"
                android:hint="Nº do Chip (Opcional)"
                android:textColor="@android:color/white"
                android:textColorHint="#AFAFAF"
                android:padding="12dp" />

            <!-- Botão Guardar -->
            <Button
                android:id="@+id/btnGuardarAnimal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:background="@drawable/borda_botoes"
                android:text="Guardar"
                android:textColor="@android:color/white"
                app:backgroundTint="@null"/>

            <!-- Botão Eliminar -->
            <Button
                android:id="@+id/btnApagarAnimal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:background="@drawable/borda_botoes_vermelho"
                android:text="Eliminar Animal"
                android:textColor="@android:color/white"
                android:visibility="gone"
                app:backgroundTint="@null"/>

        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>