<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Backup">


        <!-- página main (abertura da aplicação) -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- todas as páginas da aplicação -->
        <activity android:name=".ui.activities.HomeActivity" />
        <activity android:name=".ui.activities.RegistarActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.AboutActivity" />
        <activity android:name=".ui.activities.AgendarVacinaActivity" />
        <activity android:name=".ui.activities.AnimalActivity" />
        <activity android:name=".ui.activities.EscolhaActivity" />
        <activity android:name=".ui.activities.HistoricoActivity" />
        <activity android:name=".ui.activities.MarcarConsultaActivity" />
        <activity android:name=".ui.activities.VacinasActivity" />
        <activity android:name=".ui.activities.PerfilActivity" />
        <activity android:name=".ui.activities.DefinicoesActivity" />
        <activity android:name=".ui.activities.ConsultasActivity" />

    </application>

</manifest>